{
  "name": "bonusx-coding-challenge",
  "version": "1.0.0",
  "description": "File upload application with NestJS backend and React frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "cd packages/backend && npm run start:dev",
    "dev:frontend": "cd packages/frontend && npm start",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd packages/backend && npm run build",
    "build:frontend": "cd packages/frontend && npm run build",
    "install:all": "npm install && cd packages/backend && npm install && cd ../frontend && npm install",
    "start": "cd packages/backend && npm run start:prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 